const emojiNames = [`smile`, `sleeping`, `puke`, `angry`];

const FilterType = {
  ALL: `All`,
  WATCHLIST: `Watchlist`,
  HISTORY: `History`,
  FAVORITES: `Favorites`,
};

export {emojiNames, FilterType};
